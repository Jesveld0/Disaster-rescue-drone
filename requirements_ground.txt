# PyTorch GPU Repository (Prevents CPU-only installs)
--extra-index-url https://download.pytorch.org/whl/cu121

# Core Deep Learning
torch>=2.0.0
torchvision>=0.15.0

# Object Detection — Ultralytics RT-DETR (same model as main.py)
ultralytics>=8.2.0

# Computer Vision
opencv-python>=4.8.0

# Human Tracking
deep-sort-realtime>=1.3.2

# Math & Data (pinned below 2.0.0 to prevent OpenCV crashes)
numpy>=1.24.0,<2.0.0

# Image Processing
Pillow>=10.0.0

# Testing
pytest>=7.0.0